Simple Recipes site

Files created:
- index.html — home page with links to recipes
- style.css — basic responsive styles
- recipes/*.html — three sample recipe pages

Open `recipes-site/index.html` in a browser to view the site.

Next steps:
- Add images in `recipes-site/assets/` and reference them
- Replace Google Fonts with local fonts if offline
- Add more recipes or a JSON-driven listing
